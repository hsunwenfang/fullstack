apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: fullstack
data:
  # Defaults; override with Secret where needed
  DATABASE_URL: postgresql+psycopg://postgres:postgres@postgres:5432/appdb
  MONGO_URL: mongodb://mongo:27017
  MONGO_DB: appdb
  OPENAI_MODEL: gpt-4o-mini
